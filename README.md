1️⃣ What is the difference between var, let, and const?
Answer: var is a old style that we should avoid. let: can change the value and const: can't change the value.


2️⃣ What is the spread operator (...)?
Answer: by using spreed operator we can expand the array or object value and create a new array or object.


3️⃣ What is the difference between map(), filter(), and forEach()?
Answer: map() change the element and create a new array, filter() select element by condition and make a new new array, forEach() works for loop.


4️⃣ What is an arrow function?
Answer: arrow function is a short and morder way for Javascript function


5️⃣ What are template literals?
Answer: we can variable expression (${}) by using `` (template literals)


